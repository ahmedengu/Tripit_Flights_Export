describe('tripitflightsexport', () => {
    'use strict';

    beforeEach(module('tripitflightsexport'));

    it('should always work', () => {
        expect(true)
            .toBe(true);
    });
});
