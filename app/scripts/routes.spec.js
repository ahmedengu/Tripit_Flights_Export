describe('tripitflightsexport.routes', () => {
    'use strict';

    beforeEach(module('tripitflightsexport'));

    it('should be defined', () => {
        expect(true)
            .toBe(true);
    });
});